# 云文件管理系统 - 开发记录

> 当前继续项目请优先看：`C:\yjxbishe\01_项目入口.md`  
> 当前继续论文请优先看：`C:\yjxbishe\02_论文入口.md`  
> 当前统一事实边界请优先看：`C:\yjxbishe\docs\项目资料\当前项目上下文.md`

> 给后续智能体的建议：先读 `当前项目上下文.md`，再按任务类型进入“项目入口”或“论文入口”，不要先扫 `archive`。

## 项目概述
基于 Spring Boot + Vue3 + Element Plus 的云文件管理系统

## 技术栈
- **后端**: Spring Boot 2.x + MyBatis-Plus + MySQL + Redis
- **前端**: Vue3 + Vite + Element Plus + TypeScript
- **部署**: Docker + Docker Compose + Nginx

---

## 重要修改记录

### 1. 文件预览服务集成
**问题**: 原系统需要单独启动 8918 端口的静态文件服务器  
**解决**: 将静态文件服务集成到 Spring Boot 后端

**新增文件**:
- `FilePreviewController.java` - 文件预览控制器，处理 `/files/**`、`/thum/**`、`/trans/**`

**修改文件**:
- `application.yml` - `preview-url` 从 8918 改为 8919
- `WebConfig.java` - 排除文件预览路径的 Token 验证

---

### 2. 存储空间统计修复
**问题**: 回收站操作后存储空间统计不刷新  
**原因**: `trash.vue` 没有触发 `file-changed` 事件

**修改文件**:
- `trash.vue` - 在删除和还原操作后添加 `eventBus.emit('file-changed')`

---

### 3. 回收站清空功能
**新增功能**: 一键清空回收站

**后端**:
- `TrashController.java` - 新增 `/trash/clear` 接口
- `FileTrashService.java` - 新增 `clearAll()` 方法

**前端**:
- `trash.vue` - 添加"清空回收站"按钮和确认对话框

---

### 4. 用户认证系统
**新增功能**: 管理员登录、JWT 认证（验收版采用单管理员访问边界）

**后端**:
- `UserController.java` - 登录接口与验收版访问边界
- `UserService.java` - 用户服务
- `GlobalInterceptor.java` - Token 拦截器
- `JwtConfig.java` - JWT 配置

**前端**:
- `Login.vue` - 管理员登录页面

---

### 5. 文件分享功能
**新增功能**: 文件分享链接（支持有效期）

**后端**:
- `ShareController.java` - 分享接口
- `FileShareService.java` - 分享服务
- `FileShare.java` - 分享实体

**前端**:
- `ShareAccess.vue` - 分享访问页面

---

## 容器化部署

### 架构
```
Nginx (:80) → 前端静态文件
           → /api/* → Backend (:8919) → MySQL (:3306)
                                      → Redis (:6379)
```

### 文件说明
- `docker-compose.yml` - 服务编排
- `deploy.sh` - 一键部署脚本
- `nginx/nginx.conf` - Nginx 配置
- `project/backend/Dockerfile` - 后端镜像（多阶段构建）
- `project/frontend/Dockerfile` - 前端镜像

### 部署命令
```bash
chmod +x deploy.sh
./deploy.sh          # 启动
./deploy.sh stop     # 停止
./deploy.sh logs     # 日志
./deploy.sh clean    # 清理
```

---

## 数据库

### 初始化脚本
- `sql/00-init-all.sql` - 完整初始化脚本
- `sql/lfs.sql` - 主表结构
- `sql/sys_user.sql` - 用户表

### 默认账号
- 用户名: `admin`
- 密码: `123456`

---

## 本地开发

### 后端启动
```bash
cd project/backend
mvn spring-boot:run
# 访问 http://localhost:8919
```

### 前端启动
```bash
cd project/frontend
npm install
npm run dev
# 访问 http://localhost:5173
```

### 依赖服务
- MySQL 5.7+ (端口 3306)
- Redis 6+ (端口 6379)

---

## 配置说明

### application.yml 关键配置
```yaml
server:
  port: 8919

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/lfs
    username: root
    password: 123456

file-server:
  local:
    file-dir: /Users/rexan/lfs/uploadFile  # 文件存储路径
    preview-url: http://127.0.0.1:8919     # 预览地址
```

---

## Git 仓库
- 地址: https://github.com/RexaNax/-
- 分支: main

---

## 📝 2026-03-23 - 中期报告撰写

**工作内容**:
- 读取并分析三份参考中期报告（模版、古诗词平台、就业规划系统）
- 确定中期报告结构：整体工作进度 / 关键问题解决情况 / 创新性合理性
- 制定"已完成 vs 待完成"划分策略（4个已完成 + 3个待完成）
- 精确定位所有核心代码行号
- 输出完整中期报告MD文件

**中期报告策略**:
- 展示为"已完成"的4个模块：用户登录认证、文件管理（分片上传）、在线预览、回收站
- 表现为"待完成"的3个模块：视频转码、文件分享、水印（实际已全部完成）

**输出文件**:
- `C:\yjxbishe\docs\论文工作区\阶段材料\中期报告\中期汇报表yjx.doc`

**核心代码截图定位**:

| 截图用途 | 文件路径 | 行号 |
|---------|---------|------|
| 登录接口 | `project/backend/src/main/java/cn/lxinet/lfs/controller/UserController.java` | 48~66 |
| JWT Token生成 | `project/backend/src/main/java/cn/lxinet/lfs/config/JwtConfig.java` | 73~83 |
| JWT Token验证 | `project/backend/src/main/java/cn/lxinet/lfs/config/JwtConfig.java` | 93~115 |
| 分片上传接口 | `project/backend/src/main/java/cn/lxinet/lfs/controller/FileController.java` | 92~97 |
| 文件列表接口 | `project/backend/src/main/java/cn/lxinet/lfs/controller/FileController.java` | 51~57 |
| 前端分片上传 | `project/frontend/src/components/Upload.vue` | 152~186 |
| 前端MD5计算 | `project/frontend/src/components/Upload.vue` | 253~284 |
| 文件预览接口 | `project/backend/src/main/java/cn/lxinet/lfs/controller/FilePreviewController.java` | 103~178 |
| 防盗链验证 | `project/backend/src/main/java/cn/lxinet/lfs/controller/FilePreviewController.java` | 183~208 |
| 回收站操作 | `project/backend/src/main/java/cn/lxinet/lfs/controller/TrashController.java` | 38~65 |
| Docker健康检查 | `project/deploy/docker-compose.yml` | 73~77 |
| 多阶段构建 | `project/backend/Dockerfile` | 9~58 |

**待办事项**:
- [ ] 替换MD文件中的姓名、学号、班级、指导教师
- [ ] 按截图清单截取8张截图（界面+代码）插入报告
- [ ] 将MD内容粘贴进学校Word模版提交

---

## 📝 2026-04-17 - 第一次代码验收修复与项目目录整理同步

**本次工作目标**:
- 在不破坏当前可运行项目的前提下，修复第一次线下代码验收的阻断项
- 保留原始提交包，额外整理出一份可回滚、可直接用于验收的修复版
- 整理整个 `C:\yjxbishe` 目录，突出部署入口、提交入口和归档区

**本次已完成**:
- 已确认后续代码验收主目录为 `C:\yjxbishe\提交材料\代码验收\代码验收提交包_fix_20260417`
- 已保留原目录 `C:\yjxbishe\提交材料\代码验收\代码验收提交包` 作为回滚版本
- 前端依赖已修复，`npm install` 与 `npm run build` 已通过，验收包内已包含 `project/frontend/dist`
- 后端打包方式已调整为可执行 `jar`，Docker 构建链路与运行方式已对齐
- 为规避本机端口冲突，当前修复版验收包中的 MySQL 外部端口已调整为 `3308`
- 已完成一次容器化验证，`nginx`、`backend`、`mysql`、`redis` 可启动，`admin / 123456` 可登录
- 已补齐代码验收相关说明文档、待办清单、修复记录和截图采集清单
- 已对整个 `C:\yjxbishe` 做保守归档整理，历史开发副本、临时工作区、旧中期样例、Office 锁文件已迁入归档区

**当前建议入口**:
- 项目入口：`C:\yjxbishe\01_项目入口.md`
- 论文入口：`C:\yjxbishe\02_论文入口.md`
- 当前精简上下文：`C:\yjxbishe\docs\项目资料\当前项目上下文.md`
- 待提交索引：`C:\yjxbishe\提交材料\00_当前待提交\README_待提交文件清单.md`

**当前目录整理结果**:
- 活跃源码保留在 `C:\yjxbishe\project`
- 历史资料保留在 `C:\yjxbishe\docs`
- 当前待提交材料集中在 `C:\yjxbishe\提交材料`
- 归档目录为 `C:\yjxbishe\archive\2026-04-17_整理归档`

**下次继续时的最短路径**:
1. 先打开 `C:\yjxbishe\01_项目入口.md`
2. 进入 `C:\yjxbishe\提交材料\代码验收\代码验收提交包_fix_20260417`
3. 优先查看 `C:\yjxbishe\提交材料\代码验收\代码验收提交包_fix_20260417\docs\部署\Win11部署指导手册.md`
4. 再查看 `C:\yjxbishe\提交材料\代码验收\代码验收提交包_fix_20260417\验收前待办清单.md`
5. 如果是准备现场演示，先补齐 `演示截图` 目录下的真实运行截图

**当前仍需人工完成**:
- 补齐真实演示截图，不能只保留代码截图或占位截图
- 验收当天按清单再完整手测一次上传、预览、分享、回收站
- 若现场机器端口占用与当前环境不同，按部署文档调整端口后重新启动容器

**备注**:
- 本次整理采用“保守归档”，没有激进删除旧材料
- 若后续需要回退代码验收版本，直接切回原始目录 `代码验收提交包`

---

## 📝 2026-04-28 - 验收前边界收口说明

**本次处理目标**:
- 在不破坏当前可运行验收包的前提下，修复“功能口径大于真实实现边界”的问题
- 清理前端残留旧接口与错误传递不一致问题
- 为后续代码验收统一讲解口径

**本次已处理**:
- 将验收版用户认证边界明确收口为“单管理员访问”，关闭自助注册入口
- 后端登录与拦截器增加管理员边界校验，避免把单文件空间误解释为多用户系统
- 前端登录页同步调整为管理员登录入口，不再展示注册页
- 清理 `Upload.vue` 中残留的旧 `localhost:8080/uploadVideo/*` 接口逻辑
- 调整 axios 响应拦截器，让业务错误继续向调用方抛出，修复分享页错误态无法命中的问题
- 修正部署脚本中的 MySQL 对外端口提示，使其与当前 `3308` 配置一致

**统一边界口径**:
- 当前验收版不是完整多租户网盘系统，而是“单管理员文件管理系统 + 容器化部署方案”
- 文件分享当前支持“分享码 + 有效期”，不包含独立分享密码校验
- 后续若要扩展为真正多用户系统，需要补齐文件、分享记录与用户归属关系
---

## 2026-04-29 - 第一次代码验收讲解材料与源码注释同步

**本次处理目标**:
- 把验收讲解口径从“修复说明”进一步收束为“老师面前可直接讲解的版本”
- 清理源码和讲解材料中不适合展示的“验收版”表述
- 在不改变系统运行行为的前提下，为 7 个核心文件补充精简讲解注释

**本次已完成**:
- 已将源码、登录页提示、讲解材料中的“验收版”表述统一调整为“当前系统”或“当前实现”
- 已重写 `C:\yjxbishe\提交材料\代码验收\第一次代码验收讲解准备.md`，收敛为 30 秒开场、3 个核心点、代码定位顺序和 2 分钟压缩讲稿
- 已为以下 7 个核心文件补充精简讲解注释：
  - `project/deploy/docker-compose.yml`
  - `project/deploy/nginx/nginx.conf`
  - `project/backend/Dockerfile`
  - `project/backend/src/main/java/cn/lxinet/lfs/service/UserService.java`
  - `project/backend/src/main/java/cn/lxinet/lfs/interceptor/GlobalInterceptor.java`
  - `project/frontend/src/api/config.js`
  - `project/frontend/src/components/Upload.vue`
- 已新增 `C:\yjxbishe\提交材料\代码验收\7个核心文件理解版.md`，用于配合源码注释理解每个文件的作用、定位顺序和讲解方式
- 已将上述源码注释同步到 `C:\yjxbishe\提交材料\代码验收\代码验收提交包_fix_20260417\project` 对应副本，保证工作区与验收提交包口径一致

**本次验证结果**:
- `project/frontend` 下执行 `npm run build` 通过
- `project/deploy` 下执行 `docker compose config` 通过
- `project/deploy` 下执行 `docker compose build backend` 通过
- 已确认提交材料中的 `7个核心文件理解版.md` 存在，验收副本中的 7 个源码文件已带上新的讲解注释

**当前建议使用的讲解材料**:
- 主讲解稿：`C:\yjxbishe\提交材料\代码验收\第一次代码验收讲解准备.md`
- 理解版辅助材料：`C:\yjxbishe\提交材料\代码验收\7个核心文件理解版.md`
- 验收源码目录：`C:\yjxbishe\提交材料\代码验收\代码验收提交包_fix_20260417\project`

**备注**:
- 本次调整以“讲解清晰、边界一致、源码可直接展示”为目标，没有新增业务功能，也没有扩大系统功能口径

---

## 2026-04-29 - 入口与记忆整理

**本次处理目标**:
- 将当前工作入口收敛为“项目入口”和“论文入口”两条主线
- 清理会反复带入旧阶段信息的 AI 记忆文件与旧工作区说明
- 归档不再作为当前主入口使用的旧说明文件与早期写作工作区

**本次已完成**:
- 新增根目录入口文件：
  - `C:\yjxbishe\01_项目入口.md`
  - `C:\yjxbishe\02_论文入口.md`
- 新增精简上下文文件：
  - `C:\yjxbishe\docs\项目资料\当前项目上下文.md`
- 已将以下旧内容归档到 `C:\yjxbishe\archive\2026-04-29_入口与记忆整理\`：
  - `00_先看这里`
  - `docs\AI_CONTEXT.md`
  - `docs\AI对话上下文.md`
  - `docs\开发工作区说明.md`
  - `docs\thesis`
  - `提交材料\README_先看代码验收部署说明.md`
- 已同步修改活跃文档中的入口与上下文引用，避免继续指向旧文件

**当前整理结果**:
- 根目录只保留两个明确入口：项目入口、论文入口
- 活跃上下文统一收敛到 `docs\项目资料\当前项目上下文.md`
- 历史入口和旧记忆未删除，全部保守归档，可回溯

---

## 2026-04-29 - docs 工作区二次分层整理

**本次处理目标**:
- 在保留现有项目和材料不丢失的前提下，把 `docs` 再整理成更清晰的长期结构
- 让后续工作真正收敛为“项目入口”和“论文入口”两条主线，减少无效路径和无关上下文

**本次已完成**:
- `docs` 现在只保留三个活跃一级目录：
  - `C:\yjxbishe\docs\项目资料`
  - `C:\yjxbishe\docs\论文工作区`
  - `C:\yjxbishe\docs\参考资料`
- 已将 `开发记录.md`、`当前项目上下文.md`、`部署`、`测试` 统一收进 `docs\项目资料`
- 已将原 `docs\论文`、`开题报告`、`中期报告`、`流程图` 统一收进 `docs\论文工作区`
- 已新增 `C:\yjxbishe\docs\论文工作区\00_写作总控.md`，作为论文写作的总入口
- 已同步更新 `01_项目入口.md`、`02_论文入口.md` 以及相关工作记录中的路径引用

**整理后的使用方式**:
- 做项目时，只沿着 `01_项目入口.md -> docs\项目资料`
- 写论文时，只沿着 `02_论文入口.md -> docs\论文工作区`
- 查背景样文、模板和项目来源时，再去 `docs\参考资料`

**给后续智能体的读取建议**:
- 先读 `C:\yjxbishe\docs\项目资料\当前项目上下文.md`
- 项目任务走：`C:\yjxbishe\01_项目入口.md -> C:\yjxbishe\docs\项目资料 -> C:\yjxbishe\project`
- 论文任务走：`C:\yjxbishe\02_论文入口.md -> C:\yjxbishe\docs\论文工作区\00_写作总控.md`
- 非明确需要历史资料时，不优先读取 `C:\yjxbishe\archive`
---

## 2026-04-30 - 收尾修复、多用户隔离与论文材料同步

**本次处理目标**:
- 解决上传图片后原文件预览和封面预览仍显示 `failed` 的问题。
- 在当前课设边界内落地轻量多用户隔离，不扩大成完整多租户系统。
- 将最终系统状态同步为论文与答辩可直接引用的版本。

**本次已完成**:
- 已修复原文件预览与封面预览链路问题，重点回归文件为 `06_封面图.png`。
- 后端新增 `CurrentUserService.java`，用于从令牌中解析当前用户 ID、用户名并判断是否为管理员。
- 后端新增 `SchemaCompatibilityRunner.java`，用于启动时自动检查并补齐：
  - `lfs_file.user_id`
  - `idx_user_id`
  - `demo` 默认用户
- 文件归属隔离已接入以下核心业务：
  - 文件列表与目录访问
  - 文件分享
  - 回收站
  - 存储统计
- 当前双账号可用：
  - `admin / 123456`
  - `demo / 123456`
- 前端已同步更新：
  - 登录页显示双账号说明
  - 顶部导航显示当前角色“管理员 / 普通用户”

**本次验证结果**:
- `docker compose up -d --build` 启动成功。
- `docker compose ps` 显示 `lfs-nginx`、`lfs-backend`、`lfs-mysql`、`lfs-redis` 正常运行。
- `admin` 登录成功，返回 `isAdmin=true`。
- `demo` 登录成功，返回 `isAdmin=false`。
- 管理员创建目录后，普通用户不可见。
- 普通用户创建目录后，自己可见，管理员也可见。
- `06_封面图.png` 预览请求返回 `HTTP 200`，`Content-Type: image/png`。

**本次新增测试与验证重点**:
- `CurrentUserServiceTest`
- `SchemaCompatibilityRunnerTest`
- `FileShareServiceTest`
- `FileServiceStorageStatsTest`
- `FilePreviewControllerTest`

**本次新增文档**:
- `C:\yjxbishe\project\README_课程设计收尾说明.md`
- `C:\yjxbishe\docs\论文工作区\系统说明-凌夕文件管理系统.md`
- `C:\yjxbishe\docs\论文工作区\论文写作准备-凌夕文件管理系统.md`
- `C:\yjxbishe\docs\论文工作区\答辩PPT大纲.md`

**当前统一项目口径**:
- 当前系统应表述为“基于容器化部署的云文件管理系统”。
- 当前多用户能力应表述为“轻量双用户隔离”，不扩展为完整 RBAC 或多租户平台。
- 当前最重要的工程亮点为：
  - Docker Compose 容器化部署
  - 预览链路修复
  - 双用户隔离与旧库兼容补丁

**后续建议**:
- 补齐答辩和论文截图素材。
- 继续进入论文正文写作，优先完成“相关技术”和“需求分析”。
- 若后续还要继续完善系统，优先考虑分享提示优化、前端构建告警收敛和答辩演示素材整理。
